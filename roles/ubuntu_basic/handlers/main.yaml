---
- name: restart systemd-timesyncd
  systemd:
    name: systemd-timesyncd
    state: restarted
  become: true
  listen:
    - systemd-timesyncd reload

- name: system reboot
  reboot:
  listen:
    - system reboot
